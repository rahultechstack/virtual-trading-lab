# ---------------------------------------------------------------------------
# Runtime dependencies.
# Libraries are added in the stage that actually uses them; analytics
# analytics libraries arrive with the stage that uses them.
# ---------------------------------------------------------------------------

fastapi==0.115.6
# [standard] brings uvloop, httptools and websockets, which the live price
# stream needs.
uvicorn[standard]==0.34.0

# Settings / validation
pydantic==2.10.4
pydantic-settings==2.7.0

# Market-data HTTP client
httpx==0.28.1

# Background jobs: polls the market-data provider for the live price stream.
APScheduler==3.11.0

# Technical analysis. TA-Lib ships the C sources in its sdist and builds
# them at install time, so no system package is required.
numpy==2.2.1
pandas==2.2.3
TA-Lib==0.7.1

# Database
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
